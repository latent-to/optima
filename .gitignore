sglang/
.venv/
__pycache__/
*.pyc
*.egg-info/
optima_ledger.json
l.json
._*
.DS_Store

# Private working log — candid history, live pod access, strategy. NOT for GitHub.
WORKLOG.md

# Local miner-workbench experiments; keep out of the eventual public validator repo.
experiments/
scripts/gptoss_tp_bench.py
scripts/moe_config_microbench.py

# Local scratch / one-off experiment files (single-underscore prefix, repo root only).
/_*.py
/_*.sh
/_*.json

# Datagen workbench + its configs (local tooling, like the scripts/ entries above).
/datagen_magpie.py
/v4_engine_datagen.json

# Per-session handoff log (local, like WORKLOG.md — references box specifics / tmp paths).
/H200_SESSION.md
